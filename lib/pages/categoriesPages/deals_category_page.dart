import 'package:flutter/material.dart';

class DealsCategoryPage extends StatelessWidget {
  const DealsCategoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}